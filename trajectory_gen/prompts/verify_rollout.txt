You are a PRIVILEGED TEACHER verifying a world-model rollout and generating
a concrete-reasoning trajectory for student MLLM training.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
IMAGE LAYOUT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Your images arrive in this order (exact frame counts are given in the
INPUT section appended below):

  • Image 1          — ANCHOR FRAME: the only image the student sees.
  • Images 2 – (R+1) — ROLLOUT FRAMES (v̂): generated by the world model.
  • Images (R+2) – N — FUTURE FRAMES (v*): the actual physical outcome
                       (privileged reference, not seen by the student).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
YOUR TASK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The QUESTION, OPTIONS, GROUND-TRUTH ANSWER (y*), and SIMULATION PROMPT
USED (p_sim) are appended below.  Any previous simulation attempts are
also listed.

Step 3 — Verify the rollout (z_ver).
Compare ROLLOUT FRAMES (v̂) with FUTURE FRAMES (v*) for the specific
outcome the question asks about.

  "accept"    — The rollout is broadly consistent with v* on the task-
                relevant outcome AND provides useful concrete evidence
                for answering the question.
  "reject"    — The rollout clearly contradicts v*, misses a key
                interaction, or shows a physically implausible future.
  "uncertain" — The rollout is only partially consistent with v*
                (correct start, drifts later) or is too blurry/static
                to be informative.

Step 4 — Write the reliance statement (z_rel).
Explain in 2–3 sentences how the rollout evidence (or lack thereof)
should be combined with abstract visual reasoning to reach the answer.
Be specific: name what the rollout does or does not show, and how that
affects confidence in each option.

Step 5 — Predict the final answer (y).
Based on your analysis, select the best answer.  Your reasoning in
Step 4 must naturally lead to the correct option y*.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Return a single JSON object.  ALL text in English.

{
  "z_ver": "accept" | "reject" | "uncertain",
  "z_ver_reasoning": "One or two sentences comparing the rollout to v*
                      on the outcome relevant to the question.",
  "z_rel": "Two to three sentences describing how to integrate the
            rollout evidence with abstract reasoning to reach the answer.",
  "y": "A" | "B" | "C" | "D"
}

Output the JSON and stop.  Do not add commentary outside the JSON.
