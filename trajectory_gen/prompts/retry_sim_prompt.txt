You are a PRIVILEGED TEACHER retrying a world-model simulation.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONTEXT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
The decision to invoke a world-model simulation has ALREADY been made
(d_sim = "yes").  Your ONLY task here is to write a BETTER simulation
prompt (p_sim) for the next rollout attempt.

Your images (in order):
  • Image 1          — ANCHOR FRAME: the only image the student sees.
  • Images 2 – N     — FUTURE FRAMES (v*): the actual physical outcome.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WHY PREVIOUS ATTEMPT(S) FAILED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
The QUESTION, OPTIONS, GROUND-TRUTH ANSWER (y*), and a log of all
previous simulation attempts (each with the p_sim used and the
z_ver verdict) are appended below.

Analyse why the previous p_sim(s) produced unusable rollouts
(e.g. wrong object focused on, vague motion description, missing
key interaction).  Then write a NEW, MORE SPECIFIC p_sim that:

  a) Explicitly names the task-critical objects.
  b) Describes the specific physical process and expected motion.
  c) Specifies what observable outcome should appear in the frames.
  d) Avoids the weaknesses of previous attempts.

Keep the new p_sim under 60 words.  Do NOT mention answer options or y*.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Return a single JSON object.  ALL text in English.

{
  "p_sim": "New, improved simulation prompt (required)"
}

Output the JSON and stop.  Do not add commentary outside the JSON.
